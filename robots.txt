User-agent: *
Allow: /

# Unreleased Learn pages and /styleguide/ are kept out of search with
#   <meta name="robots" content="noindex,nofollow">
# on each page, NOT with Disallow rules here. This is deliberate: a URL
# blocked by robots.txt is never fetched, so the noindex tag is never seen,
# and the URL can still be indexed as a bare link. Crawling must stay open
# for noindex to take effect.
#
# /learn/ links only to pages marked "published": true in /learn/pages.json,
# so unreleased pages have no crawlable inbound link either.

Sitemap: https://yumigeta.com/sitemap.xml
